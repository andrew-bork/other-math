/** 
 * @typedef {{type: 0|1, next: [NFAOption]}} NFANode
 * @typedef {{query: function, next: NFANode}} NFAOption 
 */

/**
 * 
 * @param {string} pattern 
 */


const regex = (pattern) => {
    const nfatypes = {
        finish: 0,
        determined: 1,
    };


    const parse = (stuff) => {
        
    }

}